$(document).ready(function() {
	params = getUrlVars();
	// resp = api_call('mealrequest', {
 //                method: "GetRequest",
 //                requestId: params.Id,
 //					sessionId: get s
 //            });
	// get error, if it's not null show an error message and return
	// get authorization code
	// if the user doesn't have a session cookie, ask him/her to Login
	// once they login, successfully, send over the Stripe data
	// send it to server using 'host' { method: "StripeConnect", auth: params.Authorization_code }
	// if the response is a success, show it
	// if the response is a failure, show it
);